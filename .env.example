# Storyblok API access token (Published API recommended)
STORYBLOK_TOKEN=

# Optional: override the version to fetch (draft or published)
# STORYBLOK_VERSION=published

# Optional: folder within Storyblok used for tours (defaults to "tours")
# STORYBLOK_TOURS_FOLDER=tours

# PostHog analytics (client-side)
PUBLIC_POSTHOG_KEY=
# PUBLIC_POSTHOG_HOST=https://app.posthog.com
